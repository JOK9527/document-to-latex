interface:
  display_name: "Document to LaTeX"
  short_description: "Convert academic DOC/DOCX drafts into LaTeX projects"
  default_prompt: "Use $document-to-latex to convert the DOC/DOCX academic draft into a structured, compilable LaTeX project."
