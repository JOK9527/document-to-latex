# skills

Copy the `document-to-latex` skill into:

```text
skills/document-to-latex/
```

The expected entry file is:

```text
skills/document-to-latex/SKILL.md
```

Keep only assistant skills in this folder. Put source documents in `source/`.
