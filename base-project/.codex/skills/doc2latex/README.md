# doc2latex skill placeholder

Replace this placeholder with the released `document-to-latex` skill.

After installation, this folder should contain:

```text
SKILL.md
README.md
references/
scripts/
assets/
```

The folder is named `doc2latex` to match common project-local Codex skill conventions. The skill metadata inside `SKILL.md` may still use a branch-specific name such as `document-to-latex` or `doc2latex-for-nwpuers`.
